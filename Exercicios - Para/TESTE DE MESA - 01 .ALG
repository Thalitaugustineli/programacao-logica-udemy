Algoritmo "semnome"

Var
// Declarações das variáveis
     x, y, i : inteiro

Inicio
// Declando as variáveis
   x <- 3
   y <- 0

// Iniciando o Para

   para i de 0 ate x-1 faca
        escreva(i,",")
   y <- y + 5
   escreval(y)
fimpara


Fimalgoritmo